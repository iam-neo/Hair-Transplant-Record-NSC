# Google Sheets setup

Run the idempotent initializer from the Apps Script editor. It creates all headers defined in `Config.gs`. Do not reorder or rename headers manually, and do not use row numbers as identifiers. Normal staff use the application, not the spreadsheet.

Restrict spreadsheet access to service administrators. Export it regularly to a clinic-controlled backup location and test a restoration.
