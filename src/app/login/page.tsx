"use client";
import Image from "next/image";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { authApi, ApiError } from "@/lib/api/client";
import { sessionStore } from "@/lib/auth/session";
export default function LoginPage(){const router=useRouter(),[identity,setIdentity]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');setLoading(true);try{const session=await authApi.login(identity,password);sessionStore.set(session);router.replace('/dashboard')}catch(err){setError(err instanceof ApiError?err.message:'Unable to sign in. Please try again.')}finally{setLoading(false)}}return <main className="login"><aside className="login-brand"><section><Image src="/Skin_logo.png" width={285} height={241} alt="Nepalgunj Skin Center official logo" priority/><h1>Hair transplant documentation, kept secure.</h1><p>A private workspace for your clinic team to manage patients, procedures, photographs, follow-ups, and payments.</p></section></aside><section className="login-card-wrap"><form className="login-card" onSubmit={submit}><Image className="mobile-logo" src="/Skin_logo.png" width={180} height={152} alt="Nepalgunj Skin Center"/><div className="eyebrow">Secure staff access</div><h2>Welcome back</h2><p className="subtle">Sign in with your assigned clinic account.</p><label className="field">Username or email<input required autoComplete="username" value={identity} onChange={e=>setIdentity(e.target.value)} /></label><label className="field">Password<input required type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} /></label>{error&&<div role="alert" className="form-error">{error}</div>}<button className="button" style={{width:'100%',marginTop:22}} disabled={loading}>{loading?'Signing in…':'Sign in'}</button><p className="subtle" style={{fontSize:12,marginTop:22}}>Access is restricted to authorized Nepalgunj Skin Center staff.</p></form></section></main>}
