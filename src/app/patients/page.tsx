"use client";
import { FormEvent, useEffect, useState } from "react";
import { AppShell } from "@/components/layout/AppShell";
import { api } from "@/lib/api/client";
import { sessionStore, can, type StoredSession } from "@/lib/auth/session";
import type { Patient } from "@/types";
import Link from "next/link";
type PatientForm={fullName:string;dateOfBirth:string;gender:string;contactNumber:string;email:string;address:string;notes:string}; const initial:PatientForm={fullName:'',dateOfBirth:'',gender:'',contactNumber:'',email:'',address:'',notes:''};
export default function Patients(){const [patients,setPatients]=useState<Patient[]>([]),[query,setQuery]=useState(''),[error,setError]=useState(''),[open,setOpen]=useState(false),[form,setForm]=useState(initial),[saving,setSaving]=useState(false),[session,setSession]=useState<StoredSession|null>(null);const load=()=>{const s=sessionStore.get();if(s)api<{items:Patient[]}>('patients.list',{query},s.token).then(r=>setPatients(r.items)).catch(e=>setError(e.message))};useEffect(()=>{setSession(sessionStore.get());load();},[]);async function submit(e:FormEvent){e.preventDefault();const s=sessionStore.get();if(!s)return;setSaving(true);setError('');try{const check=await api<{possibleDuplicates:Patient[]}>('patients.duplicates',{fullName:form.fullName,contactNumber:form.contactNumber,email:form.email,dateOfBirth:form.dateOfBirth},s.token);if(check.possibleDuplicates.length&&!confirm('A possible duplicate was found. Create this patient anyway?'))return;await api('patients.create',form,s.token);setOpen(false);setForm(initial);load()}catch(e){setError(e instanceof Error?e.message:'Could not save patient.')}finally{setSaving(false)}}return <AppShell title="Patients"><div className="page-title"><div><h2>Patients</h2><p className="subtle">Search and maintain secure patient records.</p></div>{can(session?.user??null,'patients.create')&&<button className="button" onClick={()=>setOpen(true)}>Register patient</button>}</div>{error&&<div className="form-error">{error}</div>}<section className="panel"><div className="toolbar"><input placeholder="Search name, ID, phone or email" value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()}/><button className="button secondary" onClick={load}>Search</button></div><div className="table-wrap"><table className="table"><thead><tr><th>Patient ID</th><th>Name</th><th>Contact</th><th>Registered</th><th>Status</th><th>Last activity</th></tr></thead><tbody>{patients.length?patients.map(p=><tr key={p.id}><td>{p.id}</td><td><Link href={`/patients/${p.id}`}>{p.fullName}</Link></td><td>{p.contactNumber}</td><td>{p.registrationDate}</td><td><span className="badge">{p.status}</span></td><td>{p.lastActivity||'—'}</td></tr>):<tr><td colSpan={6} className="empty">No patients found.</td></tr>}</tbody></table></div></section>{open&&<div className="modal-backdrop"><form className="modal" onSubmit={submit}><h2>Register patient</h2><p className="subtle">A server-generated patient ID will be assigned after validation.</p><div className="grid2"><label className="field">Full name<input required value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></label><label className="field">Date of birth<input type="date" value={form.dateOfBirth} onChange={e=>setForm({...form,dateOfBirth:e.target.value})}/></label><label className="field">Gender<select value={form.gender} onChange={e=>setForm({...form,gender:e.target.value})}><option value="">Select</option><option>Female</option><option>Male</option><option>Other</option></select></label><label className="field">Contact number<input required value={form.contactNumber} onChange={e=>setForm({...form,contactNumber:e.target.value})}/></label><label className="field">Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="field">Address<input value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></label></div><label className="field">Additional notes<textarea rows={3} value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label><div className="actions"><button type="button" className="button secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="button" disabled={saving}>{saving?'Saving…':'Create patient'}</button></div></form></div>}</AppShell>}
