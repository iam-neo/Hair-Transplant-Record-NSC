"use client";
import { FormEvent, useEffect, useState } from "react";
import { AppShell } from "@/components/layout/AppShell";
import { api } from "@/lib/api/client";
import { sessionStore } from "@/lib/auth/session";
type User={id:string;name:string;email:string;role:string;permissions:string[]};
export function UsersWorkspace(){const [users,setUsers]=useState<User[]>([]),[open,setOpen]=useState(false),[error,setError]=useState(''),[form,setForm]=useState<Record<string,string>>({role:'RECEPTION'}),[saving,setSaving]=useState(false),s=sessionStore.get();const load=()=>{if(s)api<{items:User[]}>('users.list',{},s.token).then(x=>setUsers(x.items)).catch(e=>setError(e.message))};useEffect(load,[]);async function submit(e:FormEvent){e.preventDefault();if(!s)return;setSaving(true);try{await api('users.create',form,s.token);setOpen(false);setForm({role:'RECEPTION'});load()}catch(e){setError(e instanceof Error?e.message:'Could not create account.')}finally{setSaving(false)}}return <AppShell title="Administration"><div className="page-title"><div><h2>Users & access</h2><p className="subtle">Manage staff accounts and least-privilege roles.</p></div><button className="button" onClick={()=>setOpen(true)}>Add user</button></div>{error&&<div className="form-error">{error}</div>}<section className="panel"><div className="table-wrap"><table className="table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Permissions</th></tr></thead><tbody>{users.length?users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.permissions.includes('*')?'All permissions':u.permissions.length+' assigned'}</td></tr>):<tr><td colSpan={4} className="empty">No users found.</td></tr>}</tbody></table></div></section>{open&&<div className="modal-backdrop"><form className="modal" onSubmit={submit}><h2>Create staff account</h2><div className="grid2">{[['fullName','Full name'],['email','Email'],['username','Username'],['password','Temporary password']].map(([k,l])=><label className="field" key={k}>{l}<input required type={k==='password'?'password':k==='email'?'email':'text'} value={form[k]||''} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<label className="field">Role<select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}>{['SUPER_ADMIN','ADMIN','DOCTOR','ASSISTANT','RECEPTION'].map(x=><option key={x}>{x}</option>)}</select></label></div><div className="actions"><button type="button" className="button secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="button" disabled={saving}>{saving?'Creating…':'Create account'}</button></div></form></div>}</AppShell>}
