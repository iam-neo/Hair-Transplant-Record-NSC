function createPatient_(user,p){requirePermission_(user,'patients.create');if(!p.fullName||!p.contactNumber)throw clientError_('Full name and contact number are required.');var id=nextId_('HT'),now=new Date(),record={PatientID:id,FullName:String(p.fullName).trim(),DateOfBirth:p.dateOfBirth||'',Gender:p.gender||'',ContactNumber:String(p.contactNumber).trim(),Email:p.email||'',Address:p.address||'',RegistrationDate:Utilities.formatDate(now,Session.getScriptTimeZone(),'yyyy-MM-dd'),RegisteredBy:user.UserID,Status:'Active',Notes:p.notes||'',CreatedAt:now,UpdatedAt:now};append_('Patients',record);createPatientFolders_(record);audit_(user,id,'CREATE','Patient',id,'Patient registered');return ok_({id:id});}
function listPatients_(user,p){requirePermission_(user,'patients.view');var q=String(p.query||'').toLowerCase();var items=rows_('Patients').filter(function(x){return !q||[x.PatientID,x.FullName,x.ContactNumber,x.Email].join(' ').toLowerCase().indexOf(q)>=0;}).map(function(x){return {id:x.PatientID,fullName:x.FullName,dateOfBirth:x.DateOfBirth,gender:x.Gender,contactNumber:x.ContactNumber,email:x.Email,address:x.Address,registrationDate:x.RegistrationDate,status:x.Status,lastActivity:lastActivity_(x.PatientID),notes:x.Notes};});return ok_({items:items.slice(0,Number(p.limit)||50)});}
function duplicatePatients_(user,p){requirePermission_(user,'patients.create');var matches=rows_('Patients').filter(function(x){return (p.contactNumber&&String(x.ContactNumber)===String(p.contactNumber))||(p.email&&String(x.Email).toLowerCase()===String(p.email).toLowerCase())||(p.fullName&&p.dateOfBirth&&String(x.FullName).toLowerCase()===String(p.fullName).toLowerCase()&&String(x.DateOfBirth).slice(0,10)===String(p.dateOfBirth));});return ok_({possibleDuplicates:matches.map(function(x){return {id:x.PatientID,fullName:x.FullName,contactNumber:x.ContactNumber,registrationDate:x.RegistrationDate,status:x.Status};})});}
function lastActivity_(patientId){var all=rows_('ActivityLogs').filter(function(a){return a.PatientID===patientId;});return all.length?all[all.length-1].CreatedAt:'';}
function getPatient_(u,p){requirePermission_(u,'patients.view');var patient=find_('Patients','PatientID',p.patientId);if(!patient)throw clientError_('Patient not found.','NOT_FOUND');return ok_({patient:patient,activity:rows_('ActivityLogs').filter(function(a){return a.PatientID===p.patientId;}).reverse()});}
function archivePatient_(u,p){requirePermission_(u,'patients.archive');var s=sheet_('Patients'),v=s.getDataRange().getValues(),h=v[0],row=v.findIndex(function(r,i){return i>0&&r[h.indexOf('PatientID')]===p.patientId;});if(row<1)throw clientError_('Patient not found.','NOT_FOUND');s.getRange(row+1,h.indexOf('Status')+1).setValue('Archived');s.getRange(row+1,h.indexOf('UpdatedAt')+1).setValue(new Date());audit_(u,p.patientId,'ARCHIVE','Patient',p.patientId,'Patient archived');return ok_({id:p.patientId});}
function activityList_(u,p){requirePermission_(u,'audit_logs.view');return ok_({items:rows_('ActivityLogs').filter(function(x){return !p.patientId||x.PatientID===p.patientId;}).reverse().slice(0,Number(p.limit)||100)});}
